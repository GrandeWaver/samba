## PRZYDATNE:
net use * /delete

KOK 1:
- założenie konta Digital Dcean
KOK 2:
- zbudowanie dropletu
- dodanie pamięci
KROK 3: 
- konfiguracja:

POLECENIA:
sudo apt-get update
sudo apt-get upgrade --yes
apt-get install acl
sudo apt install samba --yes
systemctl status smbd --no-pager -l
sudo systemctl enable --now smbd
sudo ufw allow samba

sudo mkdir -p /samba/dysk
sudo nano /etc/samba/smb.conf

[dysk]
comment = Dysk udostępniony pracownikom
path = /samba/dysk
read only = no
writable = yes
browseable = yes
guest ok = no
# valid users = @root @pracownik1

# szybki check
testparm
# restart
sudo systemctl restart smbd


# ROOT
sudo smbpasswd -a root
> hasło
mkdir /samba/dysk/root
sudo setfacl -R -m "u:root:rwx" /samba/dysk/root

# FOLDER DLA WSZYSTKICH
sudo mkdir -p /samba/dysk/udostępnione

# PORACOWNIK1
sudo adduser pracownik1
> hasło
sudo smbpasswd -a pracownik1
> hasło

^ prawa wykonywania, odczytu
sudo setfacl -R -m "u:pracownik1:rx" /samba/dysk

mkdir /samba/dysk/pracownik1
sudo setfacl -R -m "u:pracownik1:rwx" /samba/dysk/pracownik1



